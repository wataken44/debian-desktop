- name: install japanese input and related packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  with_items:
    - fctix5
    - fctix5-configtool
    - fctix5-mozc
    - fcitx5-frontend-all
    - fonts-noto-cjk
    - fonts-noto-color-emoji
